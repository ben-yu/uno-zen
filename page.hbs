{{!< default}}
{{#post}}
    <article class="post">
        <header class="post-header"><h1>{{title}}</h1></header>

        <section class="{{post_class}}">
          {{content}}
        </section>

    </article>
{{/post}}
